---
"@poncho-ai/cli": patch
---

Fix web UI not connecting to subagent event stream when viewing an active subagent thread. The streaming guard now allows connecting to a different conversation's stream.
